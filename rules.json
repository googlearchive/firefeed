{
  "rules": {
    "people": {
      ".read": "true",
      "$userid": {
        ".write": "$userid == auth.id"
      }
    },
    "users": {
      "$userid": {
        ".read": "$userid == auth.id",
        ".write": "$userid == auth.id",
        "following": {
          "$followerid": {
            ".validate": "newData.isBoolean() && root.child('people').hasChild($followerid)"
          }
        },
        "followers": {
          "$follower": {
            ".write": "$follower == auth.id"
          }
        },
        "sparks": {
          ".read": "root.child('users').child($userid).child('followers').hasChild(auth.id)"
        },
        "stream": {
          ".write": "root.child('users').child($userid).child('following').hasChild(auth.id)",
          "$sparkid": {
            ".write": "root.child('sparks').child($sparkid).child('author').val() == auth.id"
          }
        }
      }
    },
    "sparks": {
      ".read": "true",
      "$sparkid": {
        ".write": "data.val() == null",
        ".validate": "newData.hasChildren(['author', 'content'])",
        "author": {
          ".validate": "newData.val() == auth.id"
        },
        "content": {
          ".validate": "newData.isString()"
        }
      }
    }
  }
}