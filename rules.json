{
  "rules": {
    "users": {
      "$userid": {
        ".read": "$userid == auth.userid",
        "sparks": {
          ".write": "$userid == auth.userid"
        },
        "follows": {
          ".write": "$userid == auth.userid",
          "$follow": {
            ".validate": "root.child('users').hasChild($follow) == true"
          }
        },
        "followers": {
          "$follower": {
            ".write": "$follower == auth.userid"
          }
        },
        "stream": {
          "$sparkid": {
            ".write": "auth.user == $userid || root.child('users').child($userid).child('follows').hasChild(auth.user) == true",
            ".validate": "data.val() == null && newData.hasChildren(['userid', 'sparkid'])",
            "userid": {
              ".validate": "newData.val() == auth.user"
            }
          }
        }
      }
    },
    "sparks": {
      "$sparkid": {
        ".write": "data.val() == null",
        ".validate": "newData.hasChildren['author', 'content']",
        "author": {
          ".validate": "newData.val() == auth.user"
        },
        "content": {
          ".validate": "newData.val() == true"
        }
      }
    }
  }
}