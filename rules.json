{
  "rules": {
    "people": {
      ".read": "true",
      "$userid": {
        ".write": "$userid == auth.userid"
      }
    },
    "users": {
      ".read": "true",
      "$userid": {
        "following": {
          ".write": "$userid == auth.userid",
          "$followerid": {
            ".validate": "newData.isBoolean() && root.child('people').child($followerid).val() == true"
          }
        },
        "followers": {
          "$follower": {
            ".write": "$follower == auth.userid"
          }
        },
        "sparks": {
          ".read": "root.child('users').child($userid).child('followers').child(auth.userid).val() == true",
          ".write": "$userid == auth.userid"
        },
        "stream": {
          ".read": "$userid == auth.userid",
          ".write": "$userid == auth.userid || root.child('users').child($userid).child('following').child(auth.userid).val() == true",
          "$sparkid": {
            ".validate": "data.val() == null && ($userid == auth.userid || root.child('sparks').child($sparkid).child('author').val() == auth.userid)"
          }
        }
      }
    },
    "sparks": {
      ".read": "true",
      "$sparkid": {
        ".write": "data.val() == null",
        ".validate": "newData.hasChildren(['author', 'content'])",
        "author": {
          ".validate": "newData.val() == auth.userid"
        },
        "content": {
          ".validate": "newData.isString()"
        }
      }
    }
  }
}